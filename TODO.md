# KẾ HOẠCH HOÀN THIỆN PROJECT\n\n## ✅ Đã fix\n- [x] Database data.sql\n- [x] Role.java JPA error\n- [x] Logout (delete refresh token)\n\n## 🔄 Chờ implement (stubs)\n1. **AuthServiceImpl**:\n   - verifyEmail(String token) → Skip (no table/logic)\n   - resendVerificationEmail(String email) → Send email\n   - changePassword(String old, String new) → Validate old pass\n   - updateMyProfile(UpdateUserRequest)\n2. **CartServiceImpl**:\n   - updateQuantity(Long bookId, int quantity)\n   - clearCart() → Delete all cart items\n   - checkout() → Create order\n\n## Followup\n- mvn clean compile\n- Test Swagger all APIs\n\nApprove để proceed?
